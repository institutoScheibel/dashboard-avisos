{
  "name": "dashboard-avisos-enfermeira",
  "version": "1.0.0",
  "description": "Dashboard visual para pacientes que atingiram dias de notificação",
  "scripts": {
    "prestart": "node scripts/generate-config.js",
    "start": "serve -l tcp://0.0.0.0:3333",
    "build": "node scripts/generate-config.js"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
